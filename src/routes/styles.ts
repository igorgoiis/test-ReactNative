import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  WelcomeText: {
    fontSize: 18,
    color: '#fff',
  },

  ButtonText: {
    fontSize: 18,
    color: '#fff',
  },
});

export default styles;
